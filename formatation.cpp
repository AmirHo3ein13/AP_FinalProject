#include "formatation.h"


Formatation::Formatation(Player *p1, Player *p2, Player *p3, Player *p4, Player *p5)
{
    //open a scene, for every format in Fomatation.txt, set player positions
    //show to the Player
    //Let him choose
    //Set Players pos according to player's choicel.
}
